{
  "name": "cur-env-bin",
  "version": "1.0.1",
  "description": "Automatically find files that match the current environment.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node-bin",
    "cross-platform",
    "shim",
    "bin"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.3.4",
    "https-proxy-agent": "^5.0.1",
    "user-agents": "^1.0.1330"
  },
  "devDependencies": {
    "decompress": "^4.2.1"
  }
}
